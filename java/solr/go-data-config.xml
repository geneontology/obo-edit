<dataConfig>
  <dataSource type="JdbcDataSource" 
              driver="com.mysql.jdbc.Driver"
              url="jdbc:mysql://spitz.lbl.gov/go_latest_lite" 
              user="sjcarbon" 
              password=""/>
  <document>
    <entity name="term" 
            query="SELECT id, acc, name, term_type FROM term">
      <entity name="term_synonym" 
              query="SELECT term_synonym, acc_synonym FROM term_synonym WHERE term_id='${term.id}'"/>
      <entity name="term_definition" 
              query="SELECT term_definition, term_comment FROM term_definition WHERE term_id='${term.id}'"/>
    </entity>
  </document>
</dataConfig>
