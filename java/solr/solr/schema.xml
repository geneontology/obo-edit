<?xml version="1.0" encoding="UTF-8" ?>

<schema name="golr" version="1.2">

  <types>
    <!-- Used to be in string: omitNorms="true"/> -->
    <fieldType name="string"
	       class="solr.StrField"
	       sortMissingLast="true"/>
    <fieldType name="integer"
	       class="solr.IntField"/>
    <fieldType name="text_ws"
	       class="solr.TextField"
	       positionIncrementGap="100">   
      <analyzer>
        <tokenizer class="solr.WhitespaceTokenizerFactory"/>
      </analyzer>
    </fieldType>
  </types>

  <fields>
    <!-- Both. (acc|dbxref) -->
    <field name="id"
	   type="string"
	   indexed="true"
	   stored="true"
	   required="true" />
    <!-- Both (term.name|gp.symbol) -->
    <field name="label"
	   type="text_ws"
	   indexed="true"
	   stored="true"
	   required="true" />
    <!-- Both. (term.name|gp.full_name) -->
    <field name="descriptive_name"
   	   type="text_ws"
   	   indexed="true"
   	   stored="true"
   	   required="false" />
    <!-- Both. (term_synonym|gene_product_synonym) -->
    <field name="synonym"
	   type="string"
	   indexed="true"
	   stored="true"
	   multiValued="true"
	   required="false" />
    <!-- Both. (term def|gp def (not there yet)) -->
    <field name="description"
	   type="text_ws"
	   indexed="true"
	   stored="true"
	   required="false" />
    <!-- Both. (term.term_type|gp type) -->
    <field name="type"
	   type="string"
	   indexed="true"
	   stored="true"
	   required="true" />
    <!-- Both. (term.term_type|gp.db) -->
    <field name="source"
	   type="string"
	   indexed="true"
	   stored="true"
	   required="true" />
    <!-- Term. (?) -->
    <field name="subset"
	   type="string"
	   indexed="true"
	   stored="true"
	   multiValued="true"
	   required="false" />
    <!-- Both. (term.term_type|gp.db) -->
    <field name="date"
	   type="string"
	   indexed="true"
	   stored="true"
	   required="false" />
    <!-- Term. (acc_synonym) -->
    <field name="is_obsolete"
	   type="integer"
	   indexed="true"
	   stored="true"
	   required="false" />
    <!-- Both. (?) -->
    <field name="alternate_id"
	   type="string"
	   indexed="true"
	   stored="true"
	   multiValued="true"
 	   required="false" />
    <!-- Term. (term comment) -->
    <field name="comment"
	   type="text_ws"
	   indexed="true"
	   stored="true"
	   required="false" />
    <!-- Term. (all graph ancestors) -->
    <field name="term_closure"
	   type="string"
	   indexed="true"
	   stored="true"
	   multiValued="true"
	   required="false" />
    <!-- GP (both eventually). (species ncbi_taxa_id) -->
    <field name="taxon"
	   type="string"
	   indexed="true"
	   stored="true"
	   required="false" />
    <!-- GP (both eventually). (closure of above to be implemented) -->
    <field name="taxon_closure"
	   type="string"
	   indexed="true"
	   stored="true"
	   multiValued="true"
	   required="false" />
    <!-- GP. (terms associatied with GP - will be changed (testing)) -->
    <field name="annotation_closure"
	   type="string"
	   indexed="true"
	   stored="true"
	   multiValued="true"
	   required="false" />
  </fields>

  <uniqueKey>id</uniqueKey>

  <defaultSearchField>descriptive_name</defaultSearchField>

  <solrQueryParser defaultOperator="OR"/>

</schema>
